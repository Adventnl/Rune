// A tiny dependency package. Its top-level items are exposed to dependents
// under the module name `mathx` (so `mathx::square(x)` works).

fn square(x: u32) -> u32 {
    x * x
}

fn add(a: u32, b: u32) -> u32 {
    a + b
}

fn main() {
    print(square(5));
}
